# Add16
# print(f"HalfAdder(a=a[0], b=b[0], sum=out[0], carry=carry0);")
# for i in range(1, 16):
#     print(f"FullAdder(a=a[{i}], b=b[{i}], c=carry{i-1}, sum=out[{i}], carry=carry{i});")

# Inc16
# print(f"HalfAdder(a=in[0], b=true, sum=out[0], carry=carry0);")
# for i in range(1, 16):
#     print(f"HalfAdder(a=in[{i}], b=carry{i-1}, sum=out[{i}], carry=carry{i});")
